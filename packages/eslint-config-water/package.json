{
  "name": "@pure-org/eslint-config-water",
  "version": "0.0.1",
  "private": false,
  "description": "eslint-config",
  "main": "lib/index.js",
  "files": ["./*.js", "./*/.d.ts"],
  "types": "./index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "rm -f ./*.js && rm -f ./*.d.ts && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "author": "yidafu",
  "license": "ISC",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "dependencies": {
    "@pure-org/api": "^0.0.1"
  },
  "peerDependencies": {
    "eslint": "^8.0.0"
  }
}
